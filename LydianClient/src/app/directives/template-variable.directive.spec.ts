import { TemplateVariableDirective } from './template-variable.directive';

describe('TemplateVariableDirective', () => {
  it('should create an instance', () => {
    const directive = new TemplateVariableDirective();
    expect(directive).toBeTruthy();
  });
});
